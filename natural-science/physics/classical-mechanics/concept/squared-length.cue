package concept

squared_length: {
  content: #"""
    The squared length $(ub(A))^2 := ub(A)^2$ of a vector $ub(A)$ as the squared
    distance between the points at this tail and its tip.
  """#
  related: [
    "concept/vector"
  ]
}