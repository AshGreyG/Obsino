package concept

squared_length: {
  content: #"""
    The squared length $(ub(A))^2 := ub(A)^2$ of a vector $ub(A)$ is defined as
    the squared distance between the points at its tail and its tip.
  """#
  related: [
    "concept/vector"
  ]
}